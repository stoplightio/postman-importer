{
  "swagger": "2.0",
  "info": {
    "title": "Petstore",
    "version": "1"
  },
  "paths": {
    "/pets/{id}": {
      "get": {
        "produces": [
          "application/json"
        ],
        "responses": {
          "200": {
            "description": "Response with status 200",
            "schema": {
              "example": "Dog"
            }
          }
        }
      },
      "parameters": [
        {
          "in": "path",
          "name": "id",
          "required": true,
          "type": "integer"
        }
      ]
    }
  }
}