swagger: '2.0'
info:
  title: Petstore
  version: '1'
paths:
  '/pets/{id}':
    parameters:
      - in: path
        name: id
        required: true
        type: integer
  '/employees/{code}':
    parameters:
      - in: path
        name: code
        required: true
        type: string
  '/names/{name}':
    parameters:
      - in: path
        description: name
        name: name
        required: true
        type: string