#%RAML 1.0
title: Petstore
version: 1.0.0
/pets/{id}:
  uriParameters:
    id:
      type: integer
    code: string
  get:
    responses:
      200:
        description: Response with status 200
        body:
          application/json:
            example: Dog