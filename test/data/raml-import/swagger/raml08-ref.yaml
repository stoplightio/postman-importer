swagger: '2.0'
info:
  version: '0.1'
  title: Filesystem API
host: api.example.com
schemes:
  - https
paths:
  /files:
    get:
      operationId: GET_files
      produces:
        - application/json
      responses:
        '200':
          description: ''
          schema:
            $ref: '#/definitions/Admin User'
definitions:
  Admin User:
    type: object
    properties:
      input:
        type: string