swagger: '2.0'
info:
  version: ''
  title: API Platform V2 API
consumes:
  - application/json
produces:
  - application/json
paths: {}
definitions:
  song:
    type: object
    description: main schema
    additionalProperties: true
    title: a title
    required:
      - songTitle
      - albumId
    properties:
      songTitle:
        type: string
      albumId:
        type: string
        minLength: 36
        maxLength: 36