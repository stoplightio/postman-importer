#%RAML 1.0
title: type example
version: 1

types:
  awards:
    description:
    type: string
  ResourceLink:
   description:
   facets:
     a: string
     b: number
   properties:
     href: string
     rel:
       enum: [self, next, prev]
     method?:
       default: get