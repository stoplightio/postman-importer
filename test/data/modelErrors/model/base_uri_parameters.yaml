info:
  title: My API
  version: '1.0'
baseUri:
  host: api.github.com
  protocol: https
  uri: https://api.github.com/
protocols:
  - https
baseUriParameters:
  - definition:
      internalType: string
      name: id
      minLength: 3
      example: A
      warning:
        root: Base uri parameter unused
      error:
        example: "'id.minLength=3' i.e. string length should not be less than 3"
    name: id
    required: true
