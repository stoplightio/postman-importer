swagger: "2.0"
info:
  version: 1.0.0
  title: References to external files examples
definitions:
  Person:
    type: object
    required:
      - name
    properties:
      name:
        type: string
      address:
        $ref: '../../types/Address.yaml'
      age:
        type: integer
        format: int32
        minimum: 0
paths: {}