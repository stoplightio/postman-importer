swagger: '2.0'
info:
  title: Pagination API
  version: ''
parameters:
  trait:pagination:offset:
    default: 0
    description: Skip over a number of elements by specifying an offset value for
      the query
    type: integer
    in: query
    name: offset
    required: true
  trait:pagination:limit:
    description: Limit the number of elements on the response
    type: integer
    in: query
    name: limit
    required: true
paths: {}